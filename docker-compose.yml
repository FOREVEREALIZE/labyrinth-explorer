services:
  labyrinth-explorer:
    image: ghcr.io/foreverealize/labyrinth-explorer:latest
    pull_policy: always
    ports:
      - "8787:8787"
    environment:
      PORT: "8787"
    restart: unless-stopped
